{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability for the app window",
    "windows": ["main"],
    "remote": {
        "urls": ["http://**", "https://**"]
    },
    "permissions": ["core:default", "cors-fetch:default"]
}
